"""Smoke tests - expand once agent.py has real logic."""
import pytest


def test_placeholder():
    # Replace once the Random agent exists and can run against a
    # local environment instance.
    assert True
